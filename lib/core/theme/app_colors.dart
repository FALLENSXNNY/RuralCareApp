// RuralCare Design System — Color Tokens
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — Trust Blue
  static const Color primary = Color(0xFF1565C0);
  static const Color primaryLight = Color(0xFF1E88E5);
  static const Color primaryDark = Color(0xFF0D47A1);
  static const Color primaryContainer = Color(0xFFDCEEFF);

  // Secondary — Health Green
  static const Color secondary = Color(0xFF2E7D32);
  static const Color secondaryLight = Color(0xFF43A047);
  static const Color secondaryContainer = Color(0xFFDCEDDC);

  // Emergency Red
  static const Color emergency = Color(0xFFC62828);
  static const Color emergencyDark = Color(0xFFB71C1C);
  static const Color emergencyContainer = Color(0xFFFFEBEE);
  static const Color emergencyBackground = Color(0xFFB71C1C);

  // Surfaces
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF8F9FA);
  static const Color surfaceContainer = Color(0xFFF1F3F5);

  // Text
  static const Color textPrimary = Color(0xFF1A1C1E);
  static const Color textSecondary = Color(0xFF444748);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textOnEmergency = Color(0xFFFFFFFF);

  // Status / Feedback
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF57F17);
  static const Color info = Color(0xFF1565C0);
  static const Color error = Color(0xFFC62828);

  // Offline banner
  static const Color offlineBanner = Color(0xFFFFF176);
  static const Color offlineBannerText = Color(0xFF212121);

  // Border / Divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
